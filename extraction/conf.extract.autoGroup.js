/**
 * autogrouping autogrithm 
 * autogrouping content of the page w/o using nextUntil
 */

/*
var goodTags = ['div', 'table', 'h1', 'h2', 'h3', 'h4', 'h5', 'h6'],
	groups = {},
	first = false,
	groupTitle = '';

var content = wiki-content.children

content.children.each{

	if(first && tagName != h1) continue
	if(inArray(tagName, goodTags) === -1) continue
	
	if(tagName != h1){
		if(classAttr == table-wrap){
			currentGroup.push(this)
		}
		continue
	}
	
	if(first){
		first = false;
	}else{
		groups[groupTitle] = currentGroup
	}
	
	groupTitle = h1.text.trim
	currentGroup = []	
}
groups[groupTitle] = currentGroup		
return groups

*/
